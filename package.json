{
  "name": "ast-demo",
  "version": "0.0.1",
  "description": "The ast-grep lib demo project",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@ast-grep/napi": "^0.25.4"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
